services:
  web:
    build:
      context: .
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6380:6379"
    volumes:
      - ./volumes/redisdata:/data
      
networks:
  app-network:
    driver: bridge